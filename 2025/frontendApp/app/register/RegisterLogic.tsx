'use client';

import { FC } from 'react';

import RegisterStructure from './RegisterStructure';

const RegisterLogic: FC = () => {
  return (
    <RegisterStructure />
  );
};

export default RegisterLogic;
